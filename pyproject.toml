[project]
name = "aws-mlops"
version = "0.1.0"
description = "AWS CDK-based MLOps/LLMOps API platform in Python"
authors = ["Your Name <your@email.com>"]
readme = "README.md"
requires-python = ">=3.11"

[tool.uv]
plugins = ["uv-pre-commit"]

[project.dependencies]
aws-cdk-lib = "^2.0.0"
constructs = "^10.0.0"
pytest = "^8.0.0"
pytest-cdk-synth = "*"
black = "^24.0.0"
flake8 = "^7.0.0"
isort = "^5.13.0"
